# Git Rules

- Always work on the `claude` branch. Never commit to `master` or `omri.melcer`.
- After completing any meaningful work, commit and push to `origin/claude`.
- This branch serves as a reference solution for Omri and Micha as they solve the exercise independently.
